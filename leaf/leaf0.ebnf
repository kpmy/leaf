Ext = Ident {"," Ident}.

Module  = {CRLF} "MODULE" Ident Delimiter [Ext] Delimiter "END" Ident ".".

Delimiter = ";" | CRLF.

Ident  =  LETTER {LETTER}.

/* builtin productions */
/* CR = 0DX. */
/* LF = 0AX. */
/* LETTER = abcdefghijklmnopqrstuvwxyz ABCDEFGHIJKLMNOPQRSTUVWXYZ and other Unicode chars, hard to repeat it all  */
